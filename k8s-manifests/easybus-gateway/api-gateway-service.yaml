apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: easybus-gateway
spec:
  selector:
    app: api-gateway
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30800
  type: NodePort    # exposed so frontend can reach it