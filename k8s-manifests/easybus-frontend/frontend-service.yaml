apiVersion: v1
kind: Service
metadata:
  name: easybus-frontend
  namespace: easybus-frontend
spec:
  selector:
    app: easybus-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort